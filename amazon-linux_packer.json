{
    "builders": [{
        "type": "amazon-ebs",
        "region": "us-east-1",
        "source_ami": "ami-0de53d8956e8dcf80",
        "instance_type": "t3.micro",
        "ssh_username": "ec2-user",
        "ssh_timeout": "5m",
        "ami_name": "packer_ansible_{{ timestamp }}",
        "tags": {
          "Name": "{{user `ami_name`}}"
        },
        "snapshot_tags": {
          "Name": "packer_ansible_{{ timestamp }}"
        }
    }],
    "provisioners": [{
        "type" : "shell",
        "inline" : "sudo yum -y update"
      },
      {
        "type" : "ansible",
        "playbook_file" : "ansible/site.yml"
    }]
}
